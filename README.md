# 🚌 Интеллектуальный агент для анализа жалоб на общественный транспорт

## 🎯 Описание проекта

Система для автоматического анализа жалоб граждан на работу общественного транспорта с использованием AI-агента, работающего на казахском и русском языках.

## ✨ Основные функции

### 1. **Прием и обработка жалоб**
- Текстовый ввод жалоб на казахском/русском языке
- Голосовой ввод через микрофон
- Выбор местоположения на карте

### 2. **Извлечение структурированных данных**
Система извлекает из текста жалобы:
- **Объект**: маршрут, автобус, водитель
- **Время**: время события или подачи жалобы
- **Место**: остановка, улица, перекресток
- **Аспект**: пунктуальность, заполненность, безопасность, поведение персонала, состояние автобуса, оплата
- **Приоритет**: низкий, средний, высокий, критический
- **Участник**: водитель, кондуктор и др.

### 3. **Классификация уровня жалобы**
- **Низкий** (төмен) - единичное неудобство
- **Средний** (орташа) - повторяющаяся проблема
- **Высокий** (жоғары) - системное нарушение
- **Критический** (критикалық) - угроза безопасности

### 4. **Рекомендации на казахском языке**
AI-агент формирует рекомендации для сити-менеджеров:
- Увеличить количество автобусов
- Улучшить работу по мотивации персонала
- Проверить санитарное состояние автобуса

### 5. **Визуализация данных**
- **Гистограмма проблемных маршрутов** - топ маршрутов по количеству жалоб
- **Распределение по уровням приоритета** - стековая диаграмма
- **Частота аспектов** - категории проблем
- **Проблемные участники** - водители и кондукторы

## 🏗️ Архитектура

### Backend (Spring Boot 3.5.7)
- **Java 17**
- **PostgreSQL** (Supabase)
- **Spring Security** - авторизация с ролями (USER, ADMIN)
- **Spring Data JPA** - работа с базой данных
- **RestTemplate** - интеграция с AI-вебхуком

### Frontend (Vue 3 + Vite)
- **Vue 3** с Composition API
- **Chart.js** - визуализация данных
- **Leaflet** - карты
- **Axios** - HTTP клиент
- **Pinia** - state management

### AI Integration
- Интеграция через вебхуки
- Обработка текстовых и голосовых сообщений
- Парсинг структурированных JSON ответов
- Автоматическое обновление данных в БД

## 🗄️ Структура базы данных

### Таблица `complaints`
```sql
- id (UUID) - уникальный идентификатор
- raw_text (TEXT) - исходный текст жалобы
- route (TEXT) - номер маршрута
- object (TEXT) - объект жалобы
- time (TIMESTAMPTZ) - время события
- place (TEXT) - место
- actor (TEXT) - участник (водитель, кондуктор)
- aspect (TEXT[]) - массив аспектов
- priority (TEXT) - приоритет
- evidence (TEXT[]) - доказательства
- confidence (DOUBLE) - уверенность AI
- latitude/longitude (DOUBLE) - координаты
- user_id (BIGINT) - ID пользователя
- created_by (TEXT) - автор жалобы
- status (TEXT) - статус (NEW, IN_PROGRESS, RESOLVED)
- created_at/updated_at (TIMESTAMPTZ)
```

### Таблица `users`
```sql
- id (BIGINT) - ID пользователя
- username (VARCHAR) - имя пользователя
- email (VARCHAR) - email
- password (VARCHAR) - зашифрованный пароль
- roles (TEXT[]) - роли (USER, ADMIN)
- enabled (BOOLEAN) - активен ли аккаунт
```

## 🚀 Запуск проекта

### Требования
- Java 17+
- Maven 3.9+
- Node.js 18+
- Docker и Docker Compose

### Вариант 1: Быстрый запуск (только база данных)

```bash
# Запустить PostgreSQL с автоматической инициализацией
./start-db.sh
```

База данных будет доступна на `localhost:6789` с тестовыми пользователями:
- **Admin**: `admin` / `admin123`
- **Resident**: `resident` / `user123`

### Вариант 2: Полный запуск через Docker

```bash
# Запустить все сервисы (база, бэкенд, фронтенд)
./start-all.sh

# Остановить все
./stop-all.sh
```

### Вариант 3: Локальная разработка

```bash
# 1. Запустить базу данных
./start-db.sh

# 2. Запустить Backend
mvn clean package -DskipTests
java -jar target/Big-Data-hackathon-0.0.1-SNAPSHOT.jar
# Backend: http://localhost:8081

# 3. Запустить Frontend (в отдельном терминале)
cd frontend
npm install
npm run dev
# Frontend: http://localhost:5173
```

### Конфигурация базы данных

База данных уже настроена в `application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:6789/bdh_database
spring.datasource.username=bdh_user
spring.datasource.password=hackathon2024
```

Подробности: [DATABASE_INFO.md](DATABASE_INFO.md)

## 🎨 Интерфейс

### Для жителей (ROLE_USER)
- Чат для подачи жалоб
- Голосовой ввод
- Выбор местоположения на карте
- Просмотр своих жалоб и их статусов

### Для администраторов (ROLE_ADMIN)
- Все функции жителя
- Дашборд с аналитикой
- Таблица всех жалоб с фильтрами
- Гистограммы и графики
- Карта жалоб
- Управление статусами жалоб

## 📊 API Endpoints

### Авторизация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/auth/me` - текущий пользователь
- `POST /api/auth/logout` - выход

### Жалобы
- `GET /api/complaints` - список жалоб (admin)
- `GET /api/complaints/summary` - статистика (admin)
- `GET /api/complaints/mine` - мои жалобы (user)
- `POST /api/complaints/chat` - отправка в чат (public)
- `POST /api/complaints/submit` - подача жалобы (user)
- `POST /api/complaints/chat-voice` - голосовое сообщение (public)
- `PATCH /api/complaints/{id}/status` - обновление статуса (admin)

## 🔒 Безопасность

- **Аутентификация**: Session-based с cookies
- **Авторизация**: Role-based (USER, ADMIN)
- **Пароли**: BCrypt хеширование
- **CORS**: Настроен для frontend
- **CSRF**: Отключен для REST API

## 📈 Критерии оценки

| Критерий | Вес | Реализация |
|----------|-----|------------|
| Точность классификации уровня жалобы | 30% | ✅ AI-агент через вебхук |
| Качество извлечённых кортежей | 30% | ✅ Полная структура данных |
| Качество казахской речи и объяснимость | 20% | ✅ Рекомендации на казахском |
| Информативность визуализации | 15% | ✅ 4 типа гистограмм |
| Устойчивость к ошибкам и UX | 5% | ✅ Обработка ошибок, красивый UI |

## 🎯 Особенности реализации

1. **Открытые технологии**: Использование только open-source решений
2. **Обработка ошибок**: Корректная работа с неточными текстами
3. **Билингвальность**: Поддержка казахского и русского языков
4. **Автоматизация**: Автоматическое обновление данных из AI ответов
5. **Визуализация**: Real-time обновление графиков
6. **Адаптивность**: Responsive дизайн для всех устройств

## 📝 Лицензия

MIT License

## 👥 Команда

Разработано для хакатона по Big Data

