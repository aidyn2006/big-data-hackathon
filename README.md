# Big Data Hackathon - AI Complaint Management System

Система управления жалобами на общественный транспорт с использованием AI-агента для анализа и обработки.

## 🚀 Быстрый старт

### Запуск всего проекта одной командой

```bash
docker-compose up --build
```

Или используйте удобный скрипт:

```bash
./start.sh
```

### Остановка проекта

```bash
docker-compose down
```

Или:

```bash
./stop.sh
```

## 📦 Что включено

Проект состоит из трёх сервисов, которые запускаются автоматически:

1. **PostgreSQL Database** (порт 6789)
   - База данных с автоматической инициализацией таблиц
   - Credentials: `bdh_user` / `hackathon2024`

2. **Spring Boot Backend** (порт 8081)
   - REST API для управления жалобами
   - Интеграция с AI-агентом (n8n webhooks)
   - Поддержка текстовых, голосовых и фото жалоб

3. **Vue.js Frontend** (порт 5173)
   - Современный UI для жителей и администраторов
   - AI-чат с анимацией печати
   - Интерактивная карта
   - Дашборд с аналитикой

## 🌐 Доступ к сервисам

После запуска `docker-compose up`:

- **Фронтенд**: http://localhost:5173
- **Бэкенд API**: http://localhost:8081
- **База данных**: localhost:6789

## 👥 Пользователи по умолчанию

### Администратор
- Email: `admin@example.com`
- Пароль: `admin`

### Житель
- Email: `user@example.com`
- Пароль: `user`

## 🛠️ Разработка

### Требования

- Docker & Docker Compose
- (Опционально) Java 17, Maven 3.9+, Node.js 20+ для локальной разработки

### Локальный запуск без Docker

#### Backend
```bash
mvn spring-boot:run
```

#### Frontend
```bash
cd frontend
npm install
npm run dev
```

#### Database
Используйте существующий PostgreSQL или запустите только DB из Docker:
```bash
docker-compose up db
```

## 🔧 Конфигурация

### Переменные окружения (Backend)

Основные настройки в `application.properties`:
- `spring.datasource.url` - URL базы данных
- `app.webhook.url` - URL для текстовых жалоб
- `app.webhook.voice.url` - URL для голосовых сообщений
- `app.webhook.photo.url` - URL для фото
- `app.webhook.admin.url` - URL для админского чата

### n8n Webhooks

Для полной функциональности AI-агента необходимо настроить n8n workflows:
1. Запустите n8n локально или используйте cloud версию
2. Создайте workflows для обработки жалоб
3. Обновите webhook URLs в `application.properties`

## 📊 Функциональность

### Для жителей
- ✅ Отправка жалоб через текст, голос или фото
- ✅ Выбор местоположения на карте
- ✅ AI-анализ жалобы с рекомендациями
- ✅ Просмотр статуса своих жалоб
- ✅ Группировка похожих жалоб

### Для администраторов
- ✅ Дашборд с аналитикой
- ✅ Фильтрация жалоб по маршруту, приоритету
- ✅ Визуализация на карте
- ✅ AI-помощник для анализа трендов с анимацией печати
- ✅ Обсуждение конкретных жалоб с AI
- ✅ Графики и статистика
- ✅ Индикатор "печатает..." для AI-ответов
- ✅ Естественная скорость печати с паузами на знаках препинания

## 🧪 Тестирование

```bash
# Backend tests
mvn test

# Frontend tests
cd frontend
npm run test
```

## 📝 Структура проекта

```
.
├── src/                    # Backend (Spring Boot)
│   ├── main/
│   │   ├── java/          # Java source code
│   │   └── resources/     # Конфигурация и статика
│   └── test/              # Тесты
├── frontend/              # Frontend (Vue.js)
│   ├── src/
│   │   ├── components/   # Vue компоненты
│   │   ├── pages/        # Страницы
│   │   └── store/        # State management
│   └── Dockerfile        # Frontend Docker image
├── Dockerfile            # Backend Docker image
├── docker-compose.yml    # Оркестрация всех сервисов
├── init.sql             # SQL для инициализации БД
├── start.sh             # Скрипт запуска
└── stop.sh              # Скрипт остановки
```

## 🐛 Troubleshooting

### База данных не подключается
```bash
# Проверьте, что PostgreSQL запущен
docker-compose ps

# Посмотрите логи
docker-compose logs db
```

### Backend не запускается
```bash
# Проверьте логи
docker-compose logs backend

# Пересоберите образ
docker-compose build --no-cache backend
```

### Frontend показывает ошибки API
- Убедитесь, что backend запущен на порту 8081
- Проверьте настройки CORS в `SecurityConfig.java`

## 📄 Лицензия

MIT License

## 👨‍💻 Авторы

Big Data Hackathon Team 2024
