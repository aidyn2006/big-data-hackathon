# Voice Chat Integration

## 🎤 Добавлена функциональность голосового чата с AI

### Что было добавлено:

#### 1. **RealtimeChat.vue** 
`frontend/src/components/chat/RealtimeChat.vue`

Основной компонент голосового чата, который:
- Записывает аудио с микрофона пользователя
- Отправляет аудио на n8n webhook: `https://saafrac.app.n8n.cloud/webhook/process-audio`
- Получает и воспроизводит аудио ответ от AI
- Имеет красивую анимированную визуализацию (орб с кольцом)
- Поддерживает 3 состояния: ожидание, запись, воспроизведение

#### 2. **VoiceChatFab.vue**
`frontend/src/components/ui/VoiceChatFab.vue`

Плавающая кнопка для открытия голосового чата:
- Расположена справа внизу экрана (над обычной кнопкой чата)
- Иконка микрофона 🎤
- Стильная анимация при наведении

#### 3. **Интеграция в App.vue**

Добавлены:
```vue
<VoiceChatFab @open="openVoiceChat=true" />
<RealtimeChat v-if="openVoiceChat" @close="openVoiceChat=false" />
```

### Как работает:

1. **Пользователь нажимает на кнопку микрофона** внизу справа
2. **Открывается полноэкранный оверлей** с анимированным орбом
3. **Клик по орбу** → начинается запись аудио (орб становится зелёным)
4. **Второй клик** → запись останавливается, аудио отправляется на n8n
5. **AI обрабатывает** и возвращает аудио ответ
6. **Ответ автоматически воспроизводится** (орб становится жёлтым)
7. **Кнопка закрытия** (крестик) в правом верхнем углу

### Особенности:

✅ **Полностью адаптивный дизайн** - работает на мобильных устройствах
✅ **Звуковой сигнал** при начале записи
✅ **Визуальная обратная связь** через анимации
✅ **Обработка ошибок** - показывает понятные сообщения на русском
✅ **Прерывание воспроизведения** - можно кликнуть во время ответа AI, чтобы начать новую запись
✅ **Автоматическое управление микрофоном** - треки останавливаются после записи

### API Endpoint:

```
POST https://saafrac.app.n8n.cloud/webhook/process-audio
Content-Type: multipart/form-data
Body: audio/webm файл в поле 'data'
```

### Состояния UI:

- **Ожидание**: "Нажмите чтобы говорить"
- **Запись**: "Запись..." (зелёный пульсирующий орб)
- **Обработка**: "Обрабатываю..."
- **Воспроизведение**: "Отвечаю..." (жёлтый пульсирующий орб)
- **Ошибки**: Соответствующие сообщения на русском

### Цвета:

- Основной: `#2D9A86` (бирюзовый)
- Запись: `#20c997` (зелёный)
- Воспроизведение: `#EEFE6D` (жёлтый)
- Закрытие (hover): `#ff6b6b` (красный)

### Структура файлов:

```
frontend/src/
├── components/
│   ├── chat/
│   │   └── RealtimeChat.vue          # Основной компонент
│   └── ui/
│       └── VoiceChatFab.vue          # Кнопка запуска
└── App.vue                            # Интеграция
```

---

## 🚀 Запуск

Проект уже собран и готов к использованию. Просто:

1. Запустите проект: `npm run dev` (в папке frontend)
2. Откройте сайт
3. Нажмите на кнопку микрофона 🎤 внизу справа
4. Разрешите доступ к микрофону
5. Начните говорить с AI!

---

**Статус**: ✅ Успешно интегрировано и протестировано

