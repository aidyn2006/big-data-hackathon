# 🚀 Быстрый старт

## Запуск проекта одной командой

```bash
./start.sh
```

Или напрямую через Docker Compose:

```bash
docker-compose up --build
```

## После запуска

1. Откройте браузер: **http://localhost:5173**

2. Войдите как **Администратор**:
   - Email: `admin@bdh.kz`
   - Пароль: `admin123`

3. Или как **Житель**:
   - Email: `resident@bdh.kz`
   - Пароль: `user123`

## Остановка

```bash
./stop.sh
```

Или:

```bash
docker-compose down
```

## Просмотр логов

```bash
./logs.sh
```

Или напрямую:

```bash
docker-compose logs -f
```

## Требования

- Docker
- Docker Compose

Вот и всё! 🎉


